

# План: Улучшение интерфейса кассира

## Обзор изменений
1. Убрать отображение времени работы в реальном времени в кассе (показывать только в Z-отчёте при закрытии смены)
2. Сделать фон кассира чёрным для лучшей видимости кнопок меню
3. Улучшить стилизацию кнопок меню для контрастности на чёрном фоне

---

## 1. Убрать таймер смены из боковой панели

**Файл:** `src/pages/Cashier.tsx`

**Изменения:**
- Удалить импорт компонента `ShiftTimer`
- Удалить отображение `ShiftTimer` в боковой панели (строки 606-610)
- Время работы и заработок останутся в Z-отчёте (`ZReportDialog.tsx`) - там уже всё реализовано правильно

**Что останется:**
- Индикатор "Смена не открыта" если смена закрыта
- Кнопка "Открыть смену"
- При закрытии смены (Z-отчёт) будет показано время работы и заработок

---

## 2. Изменить фон кассира на чёрный

**Файл:** `src/pages/Cashier.tsx`

**Изменения в стилях:**
- Основной контейнер: `bg-gray-50` → `bg-black`
- Боковая панель категорий: `bg-white border-gray-200` → `bg-gray-900 border-gray-700`
- Панель корзины: `bg-white border-gray-200` → `bg-gray-900 border-gray-700`
- Текст корзины: добавить `text-white`
- Разделители: `border-gray-200` → `border-gray-700`

---

## 3. Улучшить видимость кнопок меню

**Файл:** `src/pages/Cashier.tsx`

**Изменения:**
- Карточки товаров: добавить более яркий фон и границу
- Текст товаров: сделать белым или контрастным
- Категории в боковой панели: улучшить контраст для тёмного фона

---

## Техническая реализация

### Изменения в `src/pages/Cashier.tsx`:

```tsx
// 1. Убрать импорт ShiftTimer
// Удалить строку: import { ShiftTimer } from "@/components/cashier/ShiftTimer";

// 2. Основной контейнер (строка 564)
<div className="flex h-screen w-screen bg-black">

// 3. Боковая панель категорий (строка 566)
<aside className="w-60 border-r border-gray-700 bg-gray-900 p-2 flex flex-col">

// 4. Кнопки категорий - обновить стили текста
<span className="font-medium text-white">{cat.name}</span>

// 5. Основная область товаров (строка 643)
<main className="flex-1 flex flex-col p-2 bg-black">

// 6. Карточки товаров - улучшить видимость
<Card key={item.id} className="cursor-pointer hover:shadow-lg bg-gray-800 border-gray-600 hover:bg-gray-700">
  <CardContent className="...">
    <span className="text-sm font-medium text-center text-white">{item.name}</span>
    <span className="text-xs text-gray-300">{price}</span>
  </CardContent>
</Card>

// 7. Корзина (строка 661)
<aside className="w-80 border-l border-gray-700 bg-gray-900 flex flex-col p-2">
  <span className="font-semibold text-white">Корзина</span>
  // Остальные элементы корзины также с белым текстом
```

### Сохранение функционала смены:
- **Z-отчёт остаётся без изменений** - там уже есть расчёт времени работы и заработка
- Компонент `ShiftTimer.tsx` можно удалить или оставить (не используется)
- Смена НЕ удаляется при блокировке/выходе - это уже реализовано

---

## Файлы для редактирования

| Файл | Действие |
|------|----------|
| `src/pages/Cashier.tsx` | Изменить стили фона, убрать ShiftTimer |

## Проверка после изменений
- Кассир показывает чёрный фон
- Кнопки меню хорошо видны на чёрном фоне
- Время работы НЕ показывается в реальном времени
- При закрытии смены (Z-отчёт) показывается время работы и заработок
- Смена сохраняется при блокировке экрана и выходе

